import {RFValue} from 'react-native-responsive-fontsize';

const colors = {
  primary: '#007AFF',
  secondary: '#FFC107',
  background: '#F5F5F5',
  text: '#333333',
};

const fontSizes = {
  small: RFValue(8),
  regular: RFValue(10),
  large: RFValue(14),
  xlarge: RFValue(18),
};

const iconSize = {
  small: RFValue(20),
  regular: RFValue(30),
  large: RFValue(50),
  xlarge: RFValue(80),
};

export default {colors, fontSizes, iconSize};
